Requirements
For building and running the application you need:

JDK 1.8
Maven 4
Running the application locally
There are several ways to run the application on your local machine. One way is to execute the main method in the com.jumia.phonenumberscategorization.PhoneNumbersCategorizationApplication
class from your IDE.

then,open from browser on default tomcat port:8080

Alternatively you can use the Spring Boot Maven plugin like so:

mvn spring-boot:run

You can also build the docker image and run using the following commands :
> docker build -t phone-numbers-categorization.jar .
> docker run -p 9090:8080 phone-numbers-categorization.jar

then,open the browser on port 9090


